import React from "react";
// this is common header component
const Header = ({name}) => (
    <div className='header'>
        <span className='text'>Tweet Saver</span>
    </div>
   );
   
export default Header;